<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Mail Tracker</title>
        @yield(config('mail-tracker.admin-template.styles_section'))
    </head>
    <body>
        @yield('content')
    </body>
</html>
